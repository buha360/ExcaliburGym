spring:
  datasource:
    url: "jdbc:sqlite:file:excalibur-test?mode=memory&cache=shared"
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1
      connection-init-sql: "PRAGMA foreign_keys=ON"
